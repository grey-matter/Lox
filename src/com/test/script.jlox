fun fib(n) {
    if (n <= 1) return 1;
    return fib(n - 1) + fib(n - 2);
}

// return "sdfdSF";
print "Start " + clock();
for (var i = 1;i <= 8; i = i + 1)
    print fib(i);

print "End " + clock();

// for (var i = 0;i < 5; ++i)
//    var y = 6;